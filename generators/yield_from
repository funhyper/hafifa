yield from is a shorthand for when we want to yield a value from another generator.
instead of looping on it, we can use yield from.

Example:
def gen(x: Generator[int]):
    for num in x:
        yield num
    # We can use:
    yield from x